import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#48C2C3',
      // main: '#ffff',
    },
    secondary: {
      main: '#dc004e',
    },
    
  },
});

export default theme;
